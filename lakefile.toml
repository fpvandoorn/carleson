name = "carleson"
defaultTargets = ["Carleson"]
lintDriver = "batteries/runLinter"

[leanOptions]
relaxedAutoImplicit = false
pp.unicode.fun = true
autoImplicit = false
weak.linter.flexible = true # no rigid tactic (e.g. `exact`) after a flexible tactic (e.g. `simp`)
# Enable all mathlib linters: automatically matches what mathlib uses.
weak.linter.mathlibStandardSet = true
# Disable the long line linter, for now.
weak.linter.style.longLine = false
weak.linter.style.header = false

[[require]]
name = "mathlib"
git = "https://github.com/leanprover-community/mathlib4.git"
rev = "v4.31.0"

[[require]]
name = "checkdecls"
git = "https://github.com/PatrickMassot/checkdecls.git"
rev = "master"

[[lean_lib]]
name = "Carleson"

# Lean library with just the comparator challenge file: necessary to enable running
# comparator on this repository.
[[lean_lib]]
name = "Challenge"

# Lean library with just the comparator solution file: necessary to enable running
# comparator on this repository.
[[lean_lib]]
name = "Solution"
